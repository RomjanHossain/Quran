import 'package:flutter/material.dart';

class SurahRead extends StatelessWidget {
  const SurahRead({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surah'),
      ),
      body: const Center(
        child: Text('Surah'),
      ),
    );
  }
}
